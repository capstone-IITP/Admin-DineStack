const express = require("express");
const router = express.Router();

// Placeholder for future device routes
// router.post("/auth", deviceLogin);
// router.get("/sync", syncData);

module.exports = router;
